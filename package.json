{
	"version": "0.0.0",
	"author": "letnull19a",
	"devDependencies": {
		"concurrently": "^9.1.2",
		"husky": "^9.1.7",
		"prettier": "^3.5.3"
	},
	"scripts": {
		"prepare": "husky",
		"format": "prettier --write ./**/src",
		"dev": "concurrently \"cd frontend && pnpm run dev\" \"cd server && pnpm run start:dev\""
	}
}
